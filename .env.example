# findus project - Environment Variables Template
# -------------------------------------------------------
# Copy this file to '.env' in the same directory and replace placeholder values
# with your actual configuration. The .env file should NEVER be committed to git.

# Google Gemini API credentials
# Get your API key from: https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# Logfire integration (optional)
# Get your token from: https://logfire.pydantic.dev/
LOGFIRE_KEY=your_logfire_token_here

# Django settings
# Uncomment and set these in production environments
# DEBUG=False
# SECRET_KEY=your_django_secret_key_here
# ALLOWED_HOSTS=example.com,www.example.com

# PostgreSQL Database (REQUIRED)
# Option 1: Docker PostgreSQL (Recommended for local development)
# These values match the docker-compose.yml configuration
POSTGRES_DB=findus
POSTGRES_USER=findus
POSTGRES_PASSWORD=findus_dev_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Option 2: External PostgreSQL Database
# Comment out the Docker settings above and use these for external databases
# POSTGRES_DB=your_database_name
# POSTGRES_USER=your_username
# POSTGRES_PASSWORD=your_password
# POSTGRES_HOST=your_postgres_host
# POSTGRES_PORT=5432

# Note: PostgreSQL is mandatory for this application. The app will fail to start
# without proper PostgreSQL configuration. SQLite is not supported.

# Development Settings
# Disable rate limiting for local development (set to False for production)
DISABLE_RATELIMIT=True
